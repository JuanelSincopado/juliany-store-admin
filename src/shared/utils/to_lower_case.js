/**
 * Convierte una cadena de texto a minúsculas.
 * @param {string} text - La cadena a convertir.
 * @returns {string} La cadena en minúsculas.
 */

export const toLowerCase = (text) => {
  return text.toLowerCase();
};
